// Intentionally left empty for now.
//
// We do NOT initialize the database schema from within the running application.
// Hosted DB credentials are often restricted (no CREATE EXTENSION / CREATE TABLE), and
// doing schema changes at runtime can be risky.
